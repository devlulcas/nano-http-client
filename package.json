{
  "name": "nano-http-client-monorepo",
  "version": "1.0.0",
  "description": "Nano HTTP client monorepo",
  "main": "index.js",
  "scripts": {
    "test": "turbo run test",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "publish-packages": "turbo run build lint test && changeset version && changeset publish"
  },
  "keywords": [],
  "author": "devlulcas",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.11.2"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
